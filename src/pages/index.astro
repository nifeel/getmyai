---
import { readFileSync } from "node:fs";
import { join } from "node:path";

const html = readFileSync(join(process.cwd(), "src/reference/getmyai-home.html"), "utf-8");
---

<Fragment set:html={html} />
